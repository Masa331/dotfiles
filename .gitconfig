[user]
	email = pdonat@seznam.cz
	name = Premysl Donat
[color]
	ui = auto

[alias]
    st = status
    rebx = rebase -i HEAD~10
    rhi = reset --hard HEAD~1
    rhx = reset --hard HEAD~10
    ci = commit
    vomit = commit
    br = branch
    co = checkout
    df = diff
    lg = log -p
    who = shortlog -s --
    lol = log --pretty=oneline --abbrev-commit --graph --decorate
[branch]
	autosetuprebase = always
